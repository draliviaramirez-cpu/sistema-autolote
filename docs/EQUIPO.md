

# Integrantes y roles

 Integrante: Rol / módulo asignado:

1-David Funez | Modelo de base de datos (DER + schema.sql), esqueleto del backend Express y conexión a MySQL |

2-Livia | Módulo de gestión de vehículos (rama `feature/gestion-vehiculos`) |


3-Nando (Emerson) | Autenticación (registro/login con JWT) |

> Esta tabla debe coincidir con el historial de commits de cada quien — si alguien aparece con un módulo pero no tiene commits relacionados, el ing. puede pedir aclaraciones.





cada quien trabaja en su propia rama siguiendo Gitflow (`feature/*` → `develop` → `main`), con pull request antes de fusionar a `develop`.

